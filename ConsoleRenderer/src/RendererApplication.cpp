#include "ConsoleRenderer.h"

int main()
{
	{
		ConsoleRenderer renderer{ 150, 40, L"Console App", false, false, true };
		// ConsoleRenderer renderer{ 50, 10 };
		bool running = true;
		while (running)
		{

		}
	}
	system("pause");
}